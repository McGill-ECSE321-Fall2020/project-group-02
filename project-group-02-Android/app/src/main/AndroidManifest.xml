<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectgroup02">

   <!--
   <?xml version="1.0" encoding="utf-8"?>
    <manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.projectgroup02">

        <user-permission android:name="android.permission.INTERNET"/>
        &lt;!&ndash; Existing content with <application> tag &ndash;&gt;
    </manifest>
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectgroup02Android">
        <activity
            android:name="com.example.projectgroup02.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Projectgroup02Android.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>